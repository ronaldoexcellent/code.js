const code = {read:(statements)=>{setTimeout(eval(statements));}};
/* Content */ const html = (elem, code) => {return elem.innerHTML = code;}; const css = (elem, code) => {elem.style.cssText = code;}; const text = (elem, text) => {return elem.innerText = text;};const $ = (elem) => {return document.querySelector(elem)};const $$ = (elem) => {return document.querySelectorAll(elem);}; /* Functionality */ const restrict = (obj)=>{return obj.disabled = true}; /* style */  /* Draggable ?  const drg = {x : drag, s : dragstart, o : dragover, et : drgenter, l : dragleave, ed : dragend}; */ /* Mouse  const ms = {d : mousedown, m : mousemove, ot : mouseout, or : mouseover, ms_u : mouseup}; */ /* Animate */ var $animate; $Count = 0; _auto_slide(); function _auto_slide() {for (i in $animate) {for (let A in $animate[i].children) {$animate[i].children[A].hidden = true;} $Count++; if ($Count > $animate[i].children.length) { $Count = 1; } $animate[i].children[$Count - 1].hidden = false;} } /* Triple Click */ var t_clk; var tplclick = " "; x = 0; tplclick.onclick = () => {x++; t_clk; $t_clk = t_clk.toString(); $t_cl = $t_clk.slice($t_clk.indexOf("{") + 1, $t_clk.lastIndexOf("}")); if (x == 3) {eval($t_cl)} setTimeout(function() {x = 0}, 700);}; /* Import_All */ const imp_All = ($import) => {for ($imp = 0; $imp < $import.length; $imp++) {$new_script = document.createElement('script'); $new_script.src = $import[$imp]; document.querySelector('engine').appendChild($new_script)}}; /* Visibility State */ const hide = (obj) => {return obj.hidden = true;}; const del = (obj) => {return obj.remove();}; /* Import */ const imp = (script) => {document.querySelector('engine').appendChild(script);}; /* Random */ const randomize = (obj) => {return Math.floor(Math.random() * obj);}; /* Trace & Untrace Elements */ const trace = ($trace) => {$trace.style.border = "4px solid purple"; for (_tC = 0; _tC < $trace.children.length; _tC++) {$trace.children[_tC].style.cssText = "outline: 4px solid red; border: 4px solid green;"}}; const untrace = ($trace) => {$trace.style.border = "0"; for (_tC in $trace.children) { $trace.children[_tC].style.outline = "0"; $trace.children[_tC].style.border = "0"; }}; /* Page Switcher */ function switch_page($id, $pages_class) {const cc = document.getElementsByClassName($pages_class); for (_i = 0; _i < cc.length; _i++) {cc[_i].style.display = 'none'} document.getElementById($id).style.display = 'block'; $GET_PAGE_TITLE = document.querySelector('title'); $GET_PAGE_TITLE.innerHTML = $id + " - " + app.name;} /* Audio Callback */ function voice_output($txt) {const v_OP = new SpeechSynthesisUtterance(); v_OP.lang = 'en'; v_OP.pitch = '2'; v_OP.rate = '0.5'; v_OP.volume = '1'; v_OP.text = $txt; window.speechSynthesis.speak(v_OP);} /* Popup */ const popup = (obj)=> {obj.style.cssText = "position:fixed; top:0; left:0; right:0; bottom:0;"};
const kill = (func)=>{eval("function" + func + "() {null}")}; /* transform */